{
  "$comment": "Starting point for <policy_dir>/claude/settings.json. It is intentionally workspace-relative so one access profile can be reused for several project workspaces. chmod 600 and keep it outside every workspace. Adapt and test with the installed Claude Code version; Enso checks launch plumbing, not policy meaning.",
  "$comment_write_boundary": "This conservative read-only starting point disables shell, write, notebook-edit, and web tools. It also denies parent-relative reads and sensitive Enso paths. If clients need more, add the smallest native grants supported by your installed CLI while keeping AGENTS.md, CLAUDE.md, .claude, .agents, and skills protected.",
  "$comment_native_docs": "Claude Code is authoritative: https://code.claude.com/docs/en/permissions, https://code.claude.com/docs/en/settings, https://code.claude.com/docs/en/tools-reference, and https://code.claude.com/docs/en/skills. Tool permissions do not by themselves hide native skills or slash commands; decide which native settings and scopes your installation needs, then verify them directly.",
  "disableAllHooks": true,
  "permissions": {
    "deny": [
      "Bash",
      "Edit",
      "Write",
      "NotebookEdit",
      "WebFetch",
      "WebSearch",
      "Read(../**)",
      "Read(~/.ssh/**)",
      "Read(~/.aws/**)",
      "Read(//**/.env)",
      "Read(~/.enso/config.json)",
      "Read(~/.enso/enso.db)",
      "Read(~/.enso/secrets/**)",
      "Read(~/.enso/policies/**)",
      "Read(~/.enso/jobs/**)"
    ]
  },
  "sandbox": {
    "enabled": true,
    "failIfUnavailable": true,
    "allowUnsandboxedCommands": false,
    "network": {
      "allowedDomains": [
        "api.anthropic.com"
      ],
      "strictAllowlist": true
    },
    "filesystem": {
      "denyRead": [
        "~/.ssh",
        "~/.aws",
        "~/.config",
        "~/.enso/config.json",
        "~/.enso/enso.db",
        "~/.enso/secrets",
        "~/.enso/policies",
        "~/.enso/jobs"
      ]
    },
    "credentials": {
      "envVars": [
        {
          "name": "OP_SERVICE_ACCOUNT_TOKEN",
          "mode": "deny"
        },
        {
          "name": "SLACK_BOT_TOKEN",
          "mode": "deny"
        },
        {
          "name": "SLACK_APP_TOKEN",
          "mode": "deny"
        }
      ]
    }
  }
}
