# {{workspace_name}} workspace

This is Enso workspace `{{workspace_name}}`. Record its purpose, scope, and any critical approval requirements here; ask the user when they are not yet defined.

Use `knowledge/README.md` when present as the path-and-when-to-read index for durable workspace detail. Keep reusable workspace-only procedures in `skills/`, ordinary editable output in `drafts/`, and retained inbound attachments in `uploads/`.

The active workspace policy defines authority. Treat `AGENTS.md`, `CLAUDE.md`, `.agents/`, `.claude/`, and skill definitions as control files, and change them only when the request and policy allow it.
